# Copie para .env e ajuste. O .env real nunca é versionado.
DATABASE_URL="postgresql://beautybot:senha@localhost:5432/beautybot?schema=public"

# Chave de criptografia dos segredos em store_credentials (AES-256-GCM).
# Gere com: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
CREDENTIALS_ENCRYPTION_KEY=""

# Salt do hash de IP em clicks (LGPD: o IP em claro nunca é persistido).
CLICK_IP_SALT=""
